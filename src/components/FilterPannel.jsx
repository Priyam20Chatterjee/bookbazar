export default function FilterPanner({}){
    const handleChange = (type, value) => {
        const newValue = 
    }

    return (
        <div>
            {["genere", "author", "publisher"].map(typr => (
                <div></div>
            ))}
        </div>
    )
}